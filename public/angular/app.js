// Generated by CoffeeScript 1.9.0
"use strict";

/*
  Declare app level module which depends on filters, services, and directives
 */
angular.module("myApp", ["ngRoute", "myApp.filters", "myApp.services", "myApp.directives"]).config([
  "$locationProvider", function($locationProvider) {
    $locationProvider.html5Mode(false);
    return $locationProvider.hashPrefix('!');
  }
]).config([
  "$routeProvider", function($routeProvider) {
    $routeProvider.when("/home", {
      templateUrl: "partials/home",
      controller: UsersCtrl
    });
    $routeProvider.when("/user/:userId", {
      templateUrl: "partials/user",
      controller: UserDetailCtrl
    });
    $routeProvider.when("/socket", {
      templateUrl: "partials/socket",
      controller: SocketCtrl
    });
    $routeProvider.when("/:part", {
      templateUrl: "partials/router",
      controller: RouteController
    });
    return $routeProvider.otherwise({
      redirectTo: "/home"
    });
  }
]);

angular.module("ScoreApp", ["ngRoute", "myApp.filters", "myApp.services", "ScoreApp.directives", "xeditable", 'siyfion.sfTypeahead', 'ngAnimate', 'ngSanitize']).config([
  "$routeProvider", function($routeProvider) {
    $routeProvider.when("/roster/:team", {
      templateUrl: "partials/manage/roster",
      controller: RosterController
    });
    $routeProvider.when("/:part", {
      templateUrl: "partials/router",
      controller: RouteController
    });
    return $routeProvider.otherwise({
      redirectTo: "/clock"
    });
  }
]).config([
  "$logProvider", function($logProvider) {
    return $logProvider.debugEnabled(true);
  }
]).run(function(editableOptions, editableThemes) {
  editableOptions.theme = 'bs3';
  editableThemes.bs3.inputClass = 'input-sm';
  return editableThemes.bs3.buttonsClass = 'btn-sm';
});
